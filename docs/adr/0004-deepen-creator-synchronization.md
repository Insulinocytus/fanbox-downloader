# 深化作者同步模块

作者同步通过 `CreatorSync(config, fanbox).run()` 这一小型接口执行，模块实现集中拥有基线扫描、SQLite 下载记录、文件恢复状态机、阶段顺序、重试升级和下载修复；调度模块不能单独调用发现、续传或修复阶段。基线扫描先完整记录所有历史分页中的帖子，再独立处理帖子详情和文件；分页中途失败会跳过当前作者并把已发现帖子留到后续定时任务。Fanbox 位于内部真实接缝后，生产适配器和测试适配器都只执行单次领域操作，重试与 Cloudflare 升级仍由同步模块拥有。
