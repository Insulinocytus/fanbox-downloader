## 1. 持久化状态

- [x] 1.1 在 `test_fanbox_dl.py` 添加状态加载、按作者隔离、无效 JSON 回退和原子保存测试
- [x] 1.2 在 `fanbox_dl.py` 实现版本化 `.fanbox-state.json` 的加载、校验和 `os.replace()` 原子保存
- [x] 1.3 添加并实现旧帖子目录迁移测试，只在没有有效状态时从 `<post-id>-<title>` 目录初始化 `downloaded` 状态
- [x] 1.4 添加并实现已下载目录缺失检测，使整目录删除的帖子在分页扫描前重新处理

## 2. 帖子完成状态

- [x] 2.1 添加帖子全部成功、无文件、受限和部分文件失败的状态转换测试
- [x] 2.2 调整帖子处理流程，仅在全部文件成功后记录 `downloaded`，无文件记录 `empty`，受限记录 `restricted`
- [x] 2.3 验证失败帖子不写入完成状态，下一轮会跳过现有文件并补齐缺失文件

## 3. 增量与初始化扫描

- [x] 3.1 添加普通扫描测试，覆盖未知帖子、多页新增、连续两个已知页面停止以及置顶/取消置顶后的 ID 去重
- [x] 3.2 在 `run_once()` 中接入帖子状态，并实现连续两个已知页面的快速扫描停止条件
- [x] 3.3 添加首次扫描、状态重建和初始化失败后继续完整扫描的测试
- [x] 3.4 实现按作者保存 `initialized` 标志，仅在首次运行、状态不可用或初始化未完成时禁用提前停止

## 4. 文档与验证

- [x] 4.1 更新 `README.md`，说明状态文件位置、快速扫描、状态删除重建、不自动周期性完整扫描和不完整下载重试行为
- [x] 4.2 运行完整单元测试并执行严格 OpenSpec 校验，修复所有失败
