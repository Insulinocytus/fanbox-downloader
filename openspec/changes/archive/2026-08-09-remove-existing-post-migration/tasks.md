## 1. 回归测试

- [x] 1.1 将已有目录迁移测试替换为无有效状态且已有帖子目录时仍调用正常帖子处理、并按处理结果重建状态的测试
- [x] 1.2 确认已有文件的文件级跳过行为和已下载目录缺失后的重新处理测试继续覆盖保留行为

## 2. 状态初始化

- [x] 2.1 删除从已有帖子目录写入 `downloaded` 状态的迁移函数及其启动调用
- [x] 2.2 保持无效状态立即重置为有效空状态并执行完整分页扫描，同时保留已下载目录缺失检测

## 3. 文档与验证

- [x] 3.1 更新 README 中删除或重建 `.fanbox-state.json` 后的行为说明
- [x] 3.2 运行单元测试和 OpenSpec 严格校验，确认状态重建及目录删除恢复行为符合规格
