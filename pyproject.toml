[project]
name = "fanbox-downloader"
version = "0.0.0"
requires-python = ">=3.13"
dependencies = [
    "cloakbrowser==0.5.5",
    "croniter==6.2.4",
    "curl-cffi==0.16.0",
    "playwright==1.62.0",
    "tzdata>=2024.1",
]

[tool.uv]
package = false
